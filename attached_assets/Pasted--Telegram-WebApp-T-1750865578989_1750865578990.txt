Создай веб-магазин, который будет адаптирован как Telegram WebApp (работает во встроенном браузере Telegram) и полностью совместим с Replit (должен работать после нажатия Run без лишней настройки).

🔧 Технологии:
Фронтенд: HTML5, CSS3, Vanilla JavaScript (без фреймворков, всё нативно).

Бэкенд: Flask (на Python) или Node.js (Express) — выбери, что Replit поддерживает лучше.

База данных: SQLite (база должна храниться в корне проекта).

Всё должно работать как одностраничное приложение (SPA не обязателен).

🎨 Дизайн и стилистика:
Современный, нежный, минималистичный, под стиль премиального бренда одежды.

Цветовая палитра:

Фон: #FAF6F0 (мягкий бежевый/молочный)

Основной цвет: #D1BFA3 (бежевый с золотым оттенком)

Акцент: #8C735B (шоколадный/бронзовый)

Текст: #1E1E1E (почти чёрный, но мягкий)

Шрифт: Используй Poppins, Lora или Inter Light (через Google Fonts). Нежные, без жирных заголовков.

Адаптивность под мобильные телефоны и Telegram WebApp.

📁 Структура проекта:
cpp
Копировать
Редактировать
/ (корень проекта)
│
├── index.html             // Главная страница (каталог товаров)
├── product.html           // Страница одного товара
├── add_product.html       // Форма добавления товара (админка)
│
├── static/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   └── images/
│       └── (изображения товаров)
│
├── templates/             // если Flask
│   ├── index.html
│   ├── product.html
│   └── add_product.html
│
├── app.py                 // Flask приложение
├── store.db               // SQLite база данных
└── requirements.txt       // зависимости для Replit
🛒 Функции:
📋 Каталог товаров:

Фото, название, цена, категория, описание.

Кнопка “Подробнее” ведёт на product.html.

📁 Фильтр по категориям:

Обувь, верхняя одежда, костюмы и т.д.

🔀 Сортировка:

По цене, по алфавиту, по новизне.

➕ Добавление товаров:

Простой интерфейс (add_product.html): загрузка фото, ввод названия, цены, категории, описания.

📱 Полная адаптация под Telegram WebApp и мобильные устройства.

📦 Без онлайн оплаты. Только просмотр и подготовка к заказу.

✨ Дополнительно:
При первом запуске создай базу данных store.db автоматически (таблица products).

Пример добавленного товара должен отображаться по умолчанию.

Используй placeholder-картинки или картинки из /static/images/.

Каждая карточка товара должна быть аккуратной, в мягкой рамке с тенью (box-shadow) и скруглёнными углами (border-radius: 10px).

При наведении лёгкая анимация карточки (transform: scale(1.02)).

Реализуй красивый пустой вид, если нет товаров: "Пока здесь пусто. Добавьте что-нибудь через админку."